# Scaling experiment submission

Attach the PNG generated by your command, then answer:

```text
Experiment: model size / training budget / data size

Prediction:

Independent variable:

Fixed controls:

Primary metric:

Measured result:

Was the prediction supported?

One limitation:

Smallest useful next experiment:
```

Use the exact numbers from your JSON receipt. A result supports a claim about
this setup only; it does not prove a universal scaling law.
